import React from 'react';
import LibraryView from './containers/LibraryView'; // Import the main view
import './App.css'; // Keep base styles if needed

function App() {
  // Render only the LibraryView for now
  return (
    <LibraryView />
  );
}

export default App;